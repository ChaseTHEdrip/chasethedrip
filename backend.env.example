# Backend environment variables

# Directory where uploaded files are stored (relative to where the server runs)
UPLOAD_DIR=uploads

# Comma-separated list of allowed CORS origins (no spaces)
ALLOWED_ORIGINS=http://localhost:3000

# Maximum allowed file size in bytes (default: 104857600 = 100 MB)
MAX_FILE_SIZE_BYTES=104857600

